# Copy values from the parent project .env or set them here for local runs.

STREAM_API_KEY=
STREAM_API_SECRET=
OPENAI_API_KEY=

# Optional: override if the HTTP server runs on a different host/port
VISION_AGENT_URL=http://localhost:8000
